# Step Archive

A simple TypeScript project with Vite.

## Getting Started

1. Install dependencies: `npm install`
2. Start the development server: `npm run dev`
3. Open your browser to `http://localhost:5173`

## Build

To build for production: `npm run build`